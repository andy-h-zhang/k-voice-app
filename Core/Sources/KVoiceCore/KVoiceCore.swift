/// Package-wide metadata for `KVoiceCore`.
public enum KVoiceCore {
    /// Version of the `KVoiceCore` package's public API.
    ///
    /// Bumped by hand alongside notable milestones (phases in
    /// `docs/implementation-plan.md`); not tied to a git tag scheme yet.
    public static let version = "0.1.0"
}
